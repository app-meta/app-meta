# 视频
> 简单的在线视频播放功能

## 视频配置

* `ratio` 视频比例，默认**16/9**
* `highlight` 视频高亮时间点，默认 **[]**
* `path` 视频路径，默认空
* `showInfo` 是否显示视频信息，默认 **false**
* `highlight` 视频高亮时间点，默认 **空**，若填写则被按行解析为 `{ time: 秒数, text: 高亮文本 }` 的格式

目前视频仅支持 **mp4** 格式，存储在后端 `/attach` 下（与文档附件采用同一接口），重复上传会覆盖原文件。